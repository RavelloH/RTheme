"use client";

export function UserInfo({ onClose }: { onClose: () => void }) {
  return (
    <div className="bg-background w-full h-[calc(60vh-78px)] border-t border-border shadow-lg">
      用户面板
    </div>
  );
}
